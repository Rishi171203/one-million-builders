# haus — Session Journal · 2026-06-11

**Course:** OneMillion Builders · Day 14 (pre-ship: review & harden)
**Who:** Rishi (builder) + Claude (learning orchestrator)

---

## ✅ What we did today

### Enabled "Continue with Google" sign-in (the last manual account step)
The Google button + `/auth/callback` route were already coded in earlier sessions; what
was missing was the one-time external setup. Walked Rishi through it slowly in 3 stages:

1. **Google Cloud Console** — created a free project `haus`, set up the OAuth **consent
   screen** (External audience, app name + support email).
2. **OAuth client** — created a **Web application** client; added the authorized redirect
   URI `https://ymhedokamprkqxjdjfxu.supabase.co/auth/v1/callback` (Supabase's callback);
   got the **Client ID** + **Client Secret**.
3. **Supabase** — Authentication → Providers → **Google: enabled**, pasted Client ID +
   Secret; URL Configuration → **Site URL** `http://localhost:3000`, **Redirect URLs**
   `http://localhost:3000/**`.

**Debugging:** first attempts returned
`{"code":400,"error_code":"validation_failed","msg":"Unsupported provider: provider is
not enabled"}`. Confirmed the app's `.env.local` URL (`ymhedokamprkqxjdjfxu`) matched the
dashboard project, so it wasn't a wrong-project mix-up — the Google toggle simply wasn't
**persisting** until re-saved. After a clean re-save it worked.

**Verified end-to-end in the dev server log:**
`GET /auth/callback?code=… 307` → `GET / 200` → `/dashboard` / `/admin`. The OAuth code
was exchanged for a session and routed correctly. ✅

### Confirmed where we stand (Day 14)
Read memory + `.onemillion/state.json` + the 2026-06-10 journal at session start. App is
built well ahead of the course: all 8 buyer features + AI advice layer + real photos, all
committed/pushed through `6f8d054` (+ `ef12bfe` day-pointer bump), tsc-clean.

---

## ⚠️ Noted (not fixed): Gemini returning 503
While reading the log I saw repeated `Gemini responded 503` on `/api/advice`. **503 =
Google's Gemini service was temporarily overloaded**, not a bug in our code. Our **graceful
fallback worked exactly as designed** — every `/api/advice` still returned `200` using the
deterministic verdict text, so haus never broke for the user. It just means the verdicts
were the canned ones during that window, not AI-personalized. Worth a retry later when
Google's side recovers; if 503s persist we can add a short retry/backoff.

---

### Put haus behind a login wall (marketing homepage → login/register → app)
Rishi noticed the old landing page let anyone use the *entire* advisor without an account,
so register/login had no clear point. He chose the classic "front door" pattern: a public
marketing homepage first, then login/register, then the app. Planned it in plan mode (plan:
`~/.claude/plans/proud-bubbling-wall.md`), he approved, then built it:

- **New `/advisor` protected route** — `src/app/advisor/AdvisorClient.tsx` holds the advisor UI
  (moved out of the old `page.tsx`, trimmed of the marketing hero/gallery; share link now
  builds `…/advisor?…`). `src/app/advisor/page.tsx` is a server-component guard
  (`getUser()` → `if (!user) redirect("/login")`), same pattern as the dashboards.
- **New public homepage `src/app/page.tsx`** — hero + value props + homes gallery + a closing
  CTA, with auth-aware nav: logged-out shows **Log in / Register**; logged-in shows **Open
  advisor / Dashboard / Log out**. No advisor form here anymore.
- **Login lands users in the app** — `login/page.tsx` reads `?mode=login` (opens the Log in
  tab) and `?next=` (default `/advisor`); both email + Google success redirects now go to
  `next` instead of `/dashboard`. `auth/callback/route.ts` default `next` → `/advisor`.
- **Dashboard links repointed** — "Open the advisor" + "Edit & re-run" now use `/advisor`.

**Verified (automated):** `npx tsc --noEmit` → 0 errors. Dev server: `GET /` → 200 and the
homepage no longer contains the advisor form ("Get my verdict" gone, "Get started" present);
`GET /advisor` with no auth cookie → **307 → /login** (the wall works); `/advisor` compiled
clean, no runtime errors. **Manual click-through (for Rishi):** register/login → land on
`/advisor`; run + save a verdict; Dashboard → Edit & re-run opens `/advisor?…` prefilled;
Copy link gives an `/advisor?…` URL; Google sign-in lands on `/advisor`.

> Not yet committed — commit after Rishi eyeballs the new flow in the browser.

### Rent-vs-buy chart: investigated "buy always wins" + rebuilt for readability
Rishi reported the rent-vs-buy graph "shows buying wins every time" even when changing the
sliders, and asked for a more readable chart.

- **Not a bug (verified empirically).** Ported `computeRentVsBuy` + the India affordability
  math to a throwaway node script and swept 1,260 parameter combos: **BUY wins ~1202, RENT
  wins 58** — so the model is not rigged. For the India *sample* numbers buying wins because
  `priceMax` is savings-bound at ₹35L and the sample rent (₹28k/mo) is ≈ the loan EMI
  (~₹24.3k/mo): buying is both cheaper monthly *and* builds equity. The sliders DO recompute
  (they feed `assumptions` via `useMemo`). Renting wins at the reachable extreme **Home
  grows = 0% + Investments return = 15%** (e.g. 20y: Buy ₹44L vs Rent ₹115L). Told Rishi this
  exact setting so he can watch it flip.
- **Rebuilt the chart** (`src/components/RentVsBuy.tsx`, `NetWorthChart`, still dependency-free
  SVG). Was 320×170 with only "Year 1 / Year N" labels and no scale. Now 540×300 with: a money
  Y-axis on tidy round gridlines (`niceStep` 1/2/5×10ⁿ, honest 0-baseline), year labels +
  "Years from now →" along the bottom, a shaded band between the two lines (winner-coloured),
  the **crossover point marked** with a dashed line + "they cross ~yr N", each line's **final
  value as a coloured chip** on the right, per-year hover tooltips (native `<title>`), and a
  clearer legend. tsc clean; dev server recompiled with no errors.

> Not yet committed — bundle with the login-wall change after Rishi eyeballs both.

### Homeowner coherence: role-aware routing + a discoverable homeowner path
The login wall had landed *everyone* on `/advisor` (the buyer tool) after login — wrong for
homeowners. Rishi wants a sellable product with "no gaps in any area," so we closed this.

- **New `src/lib/roles.ts` → `roleHome(role)`** — single source of truth: buyer→`/advisor`,
  owner→`/owner`, admin→`/admin`. Shared by the login client and the OAuth callback so they
  can never disagree.
- **Role-aware landing.** `login/page.tsx`: after email sign-in it looks up the profile role
  and routes via `roleHome`; sign-up routes by the chosen toggle role; an explicit `?next=`
  (deep link) still wins. `auth/callback/route.ts` (Google + email confirm) now looks up the
  role server-side and routes the same way. Login also reads `?role=owner` to open Register
  pre-set to Homeowner.
- **Homepage is role-aware + homeowner-discoverable** (`src/app/page.tsx`): fetches the role
  so the logged-in nav/hero/closing CTAs point to the right home with the right label
  ("Open the advisor" / "My homeowner tools" / "Builder dashboard"). Added a compact, amber
  "**Already own a home?**" card (shown to logged-out visitors only) → `/login?role=owner`.
  Buyer story stays the hero; homeowner remains the secondary path (per the Day 6 decision).

**Verified:** tsc 0 errors; dev server recompiled clean. `GET /` shows the homeowner card;
`/login?role=owner` → 200; `/advisor` + `/owner` still 307→/login when logged out. Full
role-landing (homeowner→/owner etc.) needs Rishi to log in and click through.

> Not yet committed — commit after Rishi eyeballs the homeowner flow.

### Premium homepage redesign ("million-dollar", motion + image in every section)
Rishi wanted haus to look like a high-end animated product: motion in every section, a real
image in every section, haus branding throughout, and a motion slideshow. Invoked the
**UI/UX Pro Max** skill → design-system rec: "Storytelling + Feature-Rich" pattern, "Liquid
Glass" style, *slower* premium motion (avoid fast/cheap). Kept haus's indigo→purple brand
(skill's navy/gold is generic). Built it:

- **New `src/components/Reveal.tsx`** — reusable scroll-reveal (fade+rise, reduced-motion aware).
- **New `src/components/HomeSlideshow.tsx`** — cinematic auto-advancing Ken Burns crossfade
  carousel of `HOME_GALLERY`, with caption, dots, prev/next, pause-on-hover, reduced-motion aware.
- **Rebuilt `src/app/page.tsx`** into a premium, section-rich homepage: gradient scroll-progress
  bar; Ken Burns hero (kept orbs + floating verdict card + coin); glass **stats strip** with
  count-up (`CountUp` via `useInView`); **"How it works"** 3 alternating image/text rows
  (RTL flip per row); the **motion slideshow**; glass **value cards** (stagger); an infinite
  **markets marquee** of city photos; the homeowner entry (now with image); a **closing CTA**
  over a photo + glass overlay; and a proper **branded footer** (logo, tagline, markets, links).
  All animations respect `prefers-reduced-motion`. Reused only verified Unsplash IDs from
  `lib/photos.ts` (no broken images). Role-aware CTAs preserved.

**Verified:** tsc 0 errors (run from haus dir — note: a skill `cd` had changed cwd); dev
server compiled clean; `GET /` 200 with all new section headings present. Visual/animation
polish + the slideshow need Rishi's eyes.

> Not yet committed — commit after Rishi reviews the new look.

### Premium pass extended to the WHOLE app (login, advisor/verdict, dashboards)
Rishi clarified he wanted the premium look on *every* screen, not just the homepage. Extended
the same design language app-wide:

- **Create-account / login (`login/page.tsx`)** — rebuilt as a premium split-screen: a cinematic
  brand panel (Ken Burns home photo + indigo/purple overlay + logo + trust bullets + market
  chips) on the left, a glass form card with a Reveal entrance on the right. All auth logic
  (tabs, role toggle, Google, `?mode/?next/?role`, role-aware redirect) preserved.
- **Advisor + verdict (`advisor/AdvisorClient.tsx`)** — added a branded PhotoBanner atop the
  form, and turned the verdict into a *moment*: a celebratory gradient header card (green for
  "buy", amber for "rent") with a spring-animated icon badge, white glass chips, a drifting
  glow, then the TypeOut verdict below. Removed the now-unused `accent` var. Reduced-motion aware.
- **Dashboards** — consistent Reveal entrances + glass cards across buyer/owner/admin; admin
  stat numbers now use the brand gradient. Data/role logic untouched.

**Verified:** tsc 0 errors; dev server compiled all pages clean; `/login`, `/login?role=owner`
→ 200, `/advisor` → 307→/login when logged out, `/` → 200. Visual review pending Rishi.

> Not committed yet — homepage redesign + this whole-app pass to be committed together once
> Rishi has reviewed the look across screens.

### Brand logo + favicon (real identity)
Used the **design** skill for logo direction; chose hand-built **SVG** over Gemini raster
(crisp at 34px nav size, theme-adaptive, doubles as favicon). Presented 3 concepts in a
preview (`branding/logo-concepts.html`): Home Tile, h-Roof Monogram, Ascend. **Rishi picked
1 + 2 → combined:** the gradient tile (1) holding the white "h-roof" monogram (2) — the letter
h whose shoulder is a house roof.
- **New `src/components/HausLogo.tsx`** — reusable SVG mark (indigo→purple gradient tile + white
  h-roof), `size` prop, unique gradient id via `useId`. Replaced the generic
  `Box + HomeRoundedIcon` logo in the homepage nav + footer, advisor nav, login (both),
  and `DashboardHeader` (all dashboards). Removed now-unused `HomeRoundedIcon` imports.
- **Favicon:** added `src/app/icon.svg` (the mark) and removed the default `favicon.ico` so the
  brand icon shows in the tab. Verified: homepage 200, the h-roof path renders, `<link rel=icon
  type=image/svg+xml>` active. tsc 0 errors.

### Advisor (the MVP) — premium redesign, top to bottom
Rishi: the advisor still looked basic and it's the MVP, so it must be the best screen. Rebuilt:
- **Cinematic hero** — the big question "Should you buy a home yet?" over a Ken-Burns HERO
  photo + brand-gradient scrim, with eyebrow, subtitle, and white-glass trust chips (free /
  AI-personalized / your currency).
- **Floated glass form card** — the form now sits in a frosted-glass card pulled *up* over the
  hero (layered look), heading changed to "Your details", with a **gradient CTA** button.
- **Result cards** — "You can afford" + "Estimated EMI" now have gradient icon badges and
  gradient numbers (glass), matching the verdict header card built earlier.
All advisor logic (state, prefill, AI call, save/share, charts) untouched; AdvisorClient is a
client component so theme-function sx is fine here. tsc 0 errors; `/advisor` compiles + still
307→/login when logged out.

### Dashboards: full rich redesign (Rishi: they looked too minimal)
First dashboard pass was too light vs the homepage. Rebuilt them to the same bar. Fixed a
runtime crash first: a **function in `sx`** (`bgcolor: (t)=>…`) can't pass from a Server
Component to MUI — replaced with static translucent tints everywhere in server pages.

- **New premium kit (client components):** `CountUp` (animate number on view), `StatCard`
  (gradient icon badge + count-up + glass + hover), `DashboardHero` (cinematic Ken-Burns home
  photo + brand-gradient scrim + eyebrow/title/subtitle/children).
- **Buyer dashboard** — `DashboardHero` (HERO photo) with an "Open the advisor" CTA, a 3-up
  StatCard row (saved verdicts / ready-to-buy / markets), and saved-advice cards rebuilt with
  a **real home thumbnail** + status chip + edit link.
- **Builder/admin** — `DashboardHero`, 4 count-up StatCards with icons (users/buyers/owners/
  assessments), and recent-activity rows with thumbnails.
- **Owner** — `DashboardHero`, 3 tool-highlight glass cards (prepay/equity/sell-vs-rent), then
  the interactive `OwnerTools`.

All pages stay Server Components for auth/data and pass only **serializable props** (no
functions) to the client kit. **Verified:** tsc 0 errors; `/dashboard` `/admin` `/owner`
compile clean and still 307→/login when logged out. Logged-in visual review pending Rishi.

### Day 9 — code review + first security fixes
Reviewed the security/logic-critical code. Foundation is solid (RLS on both tables, secrets
git-ignored + server-only, no XSS sinks, open-redirect-safe `next`, server-side guards,
graceful AI fallback). Findings: **HIGH** — role was self-assignable to admin (via signup
metadata AND via updating own profile) → any user could reach the Builder dashboard and read
every user's saved financial data. **MEDIUM** — `/api/advice` was public + unthrottled (quota
abuse). LOW — prompt-injection via `goal`, thin API input validation, admins-see-all-financials
(by design).

Fixed #1 + #2:
- **#1 (SQL, `supabase/0003_secure_roles.sql`)** — `handle_new_user()` now clamps the self-chosen
  role to buyer/owner (admin from metadata → buyer); new `guard_profile_role()` BEFORE UPDATE
  trigger reverts any role change attempted by a logged-in non-admin (allows `auth.uid() is null`
  = SQL-editor/service context, so Rishi can still promote admins manually). **Rishi must RUN
  this in the Supabase SQL editor for it to take effect**, and check the safety-net query for any
  already-rogue admins.
- **#2 (`src/app/api/advice/route.ts`)** — now requires a logged-in Supabase session (401 if not)
  + best-effort in-memory per-user rate limit (12/min → 429). Verified: unauthenticated POST now
  returns 401 (was handing out a verdict). Graceful client fallback unchanged. tsc 0 errors.

### Day 9 wrap — fixed LOW items #3 + #4
In `/api/advice`: now validates every input number (income>0, savings/rent≥0, age 18–100 →
400 on junk) and **caps the free-text `goal` to 300 chars** (limits prompt-injection surface),
normalising to only the known fields. tsc clean. That closes all four review findings.

### Day 10 — automated tests (Vitest)
Set up **Vitest** (`vitest.config.ts` with `@`→`src` alias, `test` + `test:watch` scripts).
Wrote **23 unit tests** across the 4 core math modules (`markets`, `haus`, `owner`, `rentVsBuy`)
— verdict logic, tenure clamp, monotonic affordability, prepay/refinance/equity/sell-vs-rent,
formatting, and a **regression guard that the rent-vs-buy engine is unbiased** (pro-rent
assumptions flip it to "rent"). **All 23 pass** (`npm test`). Deliverables in
`my-onemillion-build/.onemillion/`: `test-plan.md` + `test-results.md`.

### Day 10 cont. — Playwright E2E (public flows) + CI
- **E2E:** installed `@playwright/test` + Chromium; `playwright.config.ts` (Chromium-only,
  reuses the running dev server, `workers: 1`). `tests/e2e/public.spec.ts` — **4 tests, all
  pass** in a real browser: homepage loads, Register→/login, **advisor login-wall redirect**,
  login page renders. No DB writes (safe against the live Supabase). `npm run test:e2e`.
  Fixed flakiness: dev compiles routes on-demand → parallel runs starved hydration → set
  serial `workers:1` + a retry-click. Gitignored `test-results/` + `playwright-report/`.
- **CI:** `.github/workflows/haus-tests.yml` (at the repo ROOT — GitHub only reads workflows
  there) runs `npm ci` + `npm test` (unit) on every push to main + PRs, lint non-blocking.
- **Deferred (need a separate test Supabase project):** authenticated E2E (register→save) +
  running E2E in CI.

## ⏭️ Next session — still pending before ship
- **Day 9 backfill** — a formal **code review** (we only did a PRD review on Day 6).
- **Day 10 backfill** — write an **automated test suite** (currently tsc-clean + manual only).
- **Deploy to Vercel** — the LAST step, no rush (per Rishi). Will need: change Supabase Site
  URL + Redirect URLs to the production domain, and add the production redirect URI to the
  Google OAuth client.
- Optional: enable Gemini retry/backoff for transient 503s.
